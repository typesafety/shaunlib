# shaunlib

😩🤛 Work in progress 🤕

Put the bot token in a `token` file and run `cabal run` to run the thing

## Building

```shell
$ cabal build
```

## Prerequisites

* `ghc 9.10`
* `cabal >= 3.12`
* `zlib`
  * Arch Linux: `zlib`
  * Ubuntu: `zlib1g-dev`
